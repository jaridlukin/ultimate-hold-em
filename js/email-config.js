/**
 * Optional EmailJS config for silent “Share hand” send.
 *
 * Fill these from https://www.emailjs.com/ (see SHARE_EMAIL.md).
 * Public key / service / template IDs are safe to commit; they are not secrets.
 * Leave empty to use mailto: (opens the user’s email app). Copy text always works.
 */
window.UTHEmailConfig = {
  serviceId: "",
  templateId: "",
  publicKey: "",
};
